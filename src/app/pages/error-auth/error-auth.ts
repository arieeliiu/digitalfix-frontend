import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-error-auth',
  imports: [RouterLink],
  templateUrl: './error-auth.html',
})
export class PaginaErrorAuth {}